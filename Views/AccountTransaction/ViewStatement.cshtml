@using Microsoft.EntityFrameworkCore.Metadata.Internal
@model IEnumerable<bms.ViewModels.AccountTransactionVm>
@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<p>Transaction Details </p>

<div>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Type</th>
                <th>Amount</th>
                <th>Notes</th>
                <th>BalanceAfter</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var transaction in Model)
            {
                <tr>
                    <td>@transaction.TransactionId</td>
                    <td>@transaction.TransactionDate.ToString("yyyy-MM-dd HH:mm")</td>
                    <td>@transaction.TransactionType</td>
                    <td>@transaction.Amount.ToString("C")</td>
                    <td>@transaction.Notes</td>
                    <td>@transaction.BalanceAfter.ToString("C")</td>
                </tr>
            
            }
        </tbody>
    </table>
     <button type="submit" class="btn btn-outline-primary me-2" onclick="history.back()">Go Back</button>
</div>